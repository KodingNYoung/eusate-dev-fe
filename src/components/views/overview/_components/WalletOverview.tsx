import { FC } from "@/utils/types"
import React from "react"

type Props = {
  start: string
  end: string
}

const WalletOverview: FC<Props> = ({}) => {
  return <div>WalletOverview</div>
}

export default WalletOverview
