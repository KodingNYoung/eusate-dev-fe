import { PageFC } from "@/utils/types"
import React from "react"

export const metadata = {
  title: "Ticket chat",
}

const TicketChatPage: PageFC = () => {
  return <div>TicketChatPage</div>
}

export default TicketChatPage
